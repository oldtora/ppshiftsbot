# ТГ-бот: зміни PP

Бот для актуалізації змін з EMS: тг айді, активація за ключем, прив’язка ПІБ, перегляд змін та щоденні сповіщення.

## Що вміє бот

- **Активація за ключем** — одноразові ключі (ключ 1 = активний, 0 = вимкнений).
- **Вибір ПІБ** — прив’язка до профілю зі списку, завантаженого з API.
- **Мої зміни** — дата (dd-mm-yyyy), зміна (M/D/N/8/9/10), місце (SK/FD/MT).
- **Скинути ПІБ** — якщо обрано не того людину.
- **Сповіщення** — увімкнути та обрати час; приходить нагадування про зміну (якщо час ≥ 18:00 — на завтра).

Дані з EMS підтягуються з API 4 рази на день (06:00, 10:00, 14:00, 18:00). Поки API немає — використовується шаблон з мок-даними.

**Ключі активації:** не генеруються самі. Адмін запускає скрипт (`scripts/generate_keys.py`), забирає ключі з файлу `data/keys.txt` і роздає користувачам. Кожен ключ одноразовий.

---

# Тест усього функціоналу на Windows

Перевірка бота локально перед викладкою на сервер. Потрібні: Python 3.10+ та токен від [@BotFather](https://t.me/BotFather).

## 1. Відкрити проєкт у терміналі

```powershell
cd C:\Users\Yevhenii\Desktop\tgbot
```

(або свій шлях до папки `tgbot`.)

## 2. Віртуальне середовище та залежності

```powershell
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

Якщо команда `python` не знайдена — спробуй `py -3 -m venv venv`, далі `venv\Scripts\activate`.

## 3. Налаштувати .env

Скопіюй приклад і підстав свій токен:

```powershell
copy .env.example .env
notepad .env
```

У `.env` обов’язково вкажи:

```env
BOT_TOKEN=твій_токен_від_BotFather
TIMEZONE=Europe/Kyiv
```

Збережи та закрий.

## 4. Згенерувати тестові ключі

```powershell
python scripts/generate_keys.py
```

Ключі з’являться у `data\keys.txt`. Скопіюй один ключ — знадобиться для перевірки активації.

## 5. Запустити бота

```powershell
python main.py
```

У консолі має з’явитися «Бот запущено.» та повідомлення про завантаження змін. Вікно не закривати — бот працює, поки запущений процес.

## 6. Перевірити функціонал у Telegram

1. **Старт** — знайди бота в Telegram, натисни **Start** або надішли `/start`. Має прийти запрошення поділитися контактом, потім ввести ключ.
2. **Активація** — поділись контактом (або введи ключ одразу), встав ключ з `data\keys.txt` і надішли. Має прийти «Ключ активовано. Оберіть ваше ПІБ» та кнопки з іменами (з мок-даних: Yevhenii Shut, John Smith, Anna Brown).
3. **Вибір ПІБ** — натисни на своє ім’я (або будь-яке). З’явиться меню: «Мої зміни», «Сповіщення», «Скинути ПІБ».
4. **Мої зміни** — натисни «Мої зміни». Має прийти список змін за обраним ПІБ (дата, зміна, місце).
5. **Скинути ПІБ** — натисни «Скинути ПІБ», потім знову обери ПІБ зі списку. Переконайся, що «Мої зміни» показують зміни вже за новим ПІБ.
6. **Сповіщення** — натисни «Сповіщення», обери час (наприклад через 2–3 хвилини). Обраний час можна протестувати кнопкою «Тест зараз». О 18:00 та пізніше нагадування показує зміну на завтра.
7. **Повторний ключ** — з іншого акаунту (або в іншому чаті) надішли той самий ключ. Має прийти «Цей ключ вже використано.»

Зупинка бота: у вікні терміналу натисни **Ctrl+C**.

## 7. Що перевірили

| Дія | Очікування |
|-----|------------|
| /start без ключа | Запит контакту, потім ключа |
| Введення ключа | Активація → вибір ПІБ |
| Вибір ПІБ | Меню з кнопками |
| Мої зміни | Список змін за ПІБ |
| Скинути ПІБ | Знову вибір ПІБ, зміни за новим |
| Сповіщення | Вибір часу, тест, повідомлення о заданому часі (якщо є зміна) |
| Використаний ключ | «Ключ вже використано» |

Після перевірки можна викладати бота на Ubuntu за інструкцією нижче.

---

# Деплой на Ubuntu-сервер (повна інструкція)

Усі команди нижче виконуються на сервері по SSH. Репозиторій проєкту: **https://github.com/oldtora/ppshiftsbot**  
Після клонування з GitHub папка на сервері називатиметься `ppshiftsbot` (шлях: `~/ppshiftsbot` або `/home/ubuntu/ppshiftsbot`). Ім’я користувача заміни на своє (часто `ubuntu` або `root`).

---

## 1. Перший вхід на сервер: оновлення системи

Підключись по SSH, потім онови пакети та систему:

```bash
sudo apt update
sudo apt upgrade -y
```

За потреби — перезавантаження, якщо оновилося ядро:

```bash
sudo reboot
```

Після перезавантаження знову зайди по SSH.

---

## 2. Встановлення Git та інших пакетів

Git потрібен, щоб завантажити проєкт з GitHub. Якщо вже встановлений — перевір: `git --version`.

**Встановити Git, Python та інше:**

```bash
sudo apt update
sudo apt install -y python3 python3-pip python3-venv git
```

Перевірка Git:

```bash
git --version
# має показати щось на кшталт: git version 2.34.1
```

Перевірка версії Python (потрібен 3.10+):

```bash
python3 --version
```

Якщо версія Python нижча за 3.10 — додай PPA та встанови новішу:

```bash
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3.11-dev
```

Далі в командах замість `python3` використовуй `python3.11` (і `venv` створюй ним же).

---

## 3. Завантажити проєкт з GitHub до себе на сервер

**Варіант А: клонування з GitHub (рекомендовано)**

На сервері виконай:

```bash
cd ~
git clone https://github.com/oldtora/ppshiftsbot.git
cd ppshiftsbot
```

Після цього у тебе в домашній папці з’явиться каталог `ppshiftsbot` з усім кодом. Далі в інструкції шлях до проєкту — `~/ppshiftsbot` (або `/home/ubuntu/ppshiftsbot` — підстав свій логін замість `ubuntu`).

**Варіант Б: завантаження архіву/файлів з ПК**

Якщо без Git — на ПК у папці з проєктом зроби архів, перенеси на сервер і розпакуй:

На ПК (у папці з проєктом):

```bash
tar -czvf ppshiftsbot.tar.gz .
# або через scp: scp -r . user@IP_СЕРВЕРА:~/ppshiftsbot
```

На сервері:

```bash
mkdir -p ~/ppshiftsbot
cd ~/ppshiftsbot
# сюди поклади файли (scp, sftp або розпакуй ppshiftsbot.tar.gz)
```

Далі у всіх кроках використовуй шлях до цієї папки, наприклад:

```bash
cd ~/ppshiftsbot
```

---

## 4. Віртуальне середовище та залежності

```bash
cd ~/ppshiftsbot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

Перевірка:

```bash
which python
# Має бути: /home/ubuntu/ppshiftsbot/venv/bin/python
```

---

## 5. Налаштування середовища (.env)

Створи файл із змінними (токен від [@BotFather](https://t.me/BotFather)):

```bash
cd ~/ppshiftsbot
cp .env.example .env
nano .env
```

Мінімум потрібно вказати:

```env
BOT_TOKEN=123456789:ABCdefGHI...
TIMEZONE=Europe/Kyiv
```

Зберегти в nano: `Ctrl+O`, Enter, `Ctrl+X`.

Права (щоб не світилось зайвим):

```bash
chmod 600 .env
```

---

## 6. Генерація ключів активації (один раз)

```bash
cd ~/ppshiftsbot
source venv/bin/activate
mkdir -p data
python scripts/generate_keys.py
```

Ключі з’являться у `data/keys.txt` та в БД. Роздавай користувачам з цього файлу.

---

## 7. Перевірка запуску вручну

```bash
cd ~/ppshiftsbot
source venv/bin/activate
python main.py
```

У консолі має бути «Бот запущено.» та завантаження змін. Перевір бота в Telegram. Зупинка: `Ctrl+C`.

---

## 8. Запуск як сервіс (systemd) — автозапуск та перезапуск при падінні

Створи юніт-файл:

```bash
sudo nano /etc/systemd/system/tgbot.service
```

Вставити (шлях та користувача заміни на свої; якщо клонував з GitHub — папка `ppshiftsbot`):

```ini
[Unit]
Description=Telegram EMS Shifts Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/ppshiftsbot
Environment=PATH=/home/ubuntu/ppshiftsbot/venv/bin
ExecStart=/home/ubuntu/ppshiftsbot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Зберегти та вийти (`Ctrl+O`, Enter, `Ctrl+X`).

Увімкнути та запустити сервіс:

```bash
sudo systemctl daemon-reload
sudo systemctl enable tgbot
sudo systemctl start tgbot
sudo systemctl status tgbot
```

У `status` має бути `active (running)`.

---

## 9. Корисні команди по сервісу

| Дія | Команда |
|-----|---------|
| Статус | `sudo systemctl status tgbot` |
| Логи (живий вивід) | `sudo journalctl -u tgbot -f` |
| Логи (останні 100 рядків) | `sudo journalctl -u tgbot -n 100` |
| Перезапуск бота | `sudo systemctl restart tgbot` |
| Зупинка | `sudo systemctl stop tgbot` |
| Запуск | `sudo systemctl start tgbot` |
| Вимкнути автозапуск | `sudo systemctl disable tgbot` |

---

## 10. Оновлення бота на сервері

Якщо код оновлюється через git (проєкт завантажувався з GitHub):

```bash
cd ~/ppshiftsbot
git pull
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart tgbot
```

Якщо заливаєш файли вручну — скопіюй нові файли в `~/ppshiftsbot`, потім:

```bash
cd ~/ppshiftsbot
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart tgbot
```

---

## 11. Підключення реального API EMS

Коли API буде готовий:

1. У `.env` додай або зміни:
   ```env
   EMS_API_URL=https://твій-домен/api
   EMS_API_KEY=ключ
   ```
2. Формат відповіді API описано в **`docs/API_FORMAT.md`** (дата dd-mm-yyyy або dd-mm, поля fio, shift_type, location). При потребі підлаштуй `api_client.py` під свій URL та метод.

Перезапуск після змін:

```bash
sudo systemctl restart tgbot
```

---

## 12. Push-сповіщення (адмін)

Одержувачі push **обираються в боті**, не в .env.

1. У `.env` лише адміни:
   ```env
   ADMIN_IDS=твій_telegram_id
   ```
   Ці користувачі бачитимуть кнопку «📤 Push-сповіщення».

2. Адмін натискає «📤 Push-сповіщення» → бот показує **«Всім»**, список користувачів та **«Обрати кількох»**. Обираєш одержувачів → вводиш текст → бот надсилає повідомлення.

---

## Структура проєкту

```
tgbot/
├── main.py           # запуск бота
├── config.py         # налаштування з .env
├── database.py       # SQLite: ключі, користувачі, зміни, сповіщення
├── api_client.py     # запит до API EMS (шаблон + мок)
├── keyboards.py      # кнопки меню та інлайн
├── scheduler.py      # задачі: завантаження змін та сповіщення
├── handlers/         # обробники команд та кнопок
├── docs/
│   └── API_FORMAT.md # формат даних API
├── scripts/
│   └── generate_keys.py  # генерація ключів
├── data/             # bot.db, keys.txt (створюються при першому запуску)
├── .env.example
├── requirements.txt
└── README.md
```

---

## Формат даних

- **Дата:** dd-mm-yyyy (у БД і при показі «Мої зміни»); API може віддавати dd-mm або dd-mm-yyyy — нормалізується автоматично.
- **ПІБ:** рядок (наприклад Yevhenii Shut).
- **Зміна:** M, D, N, 8, 9, 10.
- **Місце:** SK, FD, MT.

Ключі в БД: поле `used` — 0 (доступний) або 1 (вже використаний).
